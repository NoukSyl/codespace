# Get an auth key from https://login.tailscale.com/admin/settings/keys
# Use a reusable, non-ephemeral key so the VM stays on your tailnet across restarts.
TS_AUTHKEY=tskey-auth-xxxxxxxxxxxx-xxxxxxxxxxxxxxxxxxxxxxxxxxxx
